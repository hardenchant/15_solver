#ifndef UTILS_H
#define UTILS_H

int mod(int a, unsigned n);  // in c++  -1 % 4 = -1 or 3 but i want = 3 always
unsigned manh_distance_in_matrix(int a, int b,
                                 int n);  // a b - coords, n - one side size

#endif